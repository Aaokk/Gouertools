{
  "name": "shuiyinimg",
  "private": true,
  "_version_sync_note": "【版本号修改】仅需改下一行 version；执行 tauri:dev / tauri:build（或 npm run sync-version）时会自动写入 Cargo.toml 与 tauri.conf.json。",
  "version": "2.0.0",
  "type": "commonjs",
  "scripts": {
    "sync-version": "node scripts/sync-version-from-package.mjs",
    "browser:dev": "vite --open --host",
    "build": "vite build",
    "tauri:before-dev": "npm run sync-version && vite --host 127.0.0.1 --port 5173 --strictPort",
    "tauri:before-build": "npm run sync-version && npm run build",
    "tauri": "tauri",
    "tauri:icon": "tauri icon ./public/favicon.svg",
    "tauri:dev": "tauri dev",
    "tauri:build": "node scripts/tauri-build.mjs"
  },
  "devDependencies": {
    "@ckpack/vue-color": "^1.5.0",
    "@tauri-apps/api": "^2.11.0",
    "@tauri-apps/cli": "^2.11.1",
    "@tauri-apps/plugin-dialog": "^2.7.1",
    "@tauri-apps/plugin-opener": "^2.5.4",
    "@tauri-apps/plugin-os": "^2.3.2",
    "@vitejs/plugin-vue": "^5.2.1",
    "exifr": "^7.1.3",
    "jszip": "^3.10.1",
    "qrcode": "^1.5.4",
    "svgo": "^4.0.1",
    "vite": "^6.0.3",
    "vue": "^3.5.13",
    "vue-router": "^4.5.0"
  }
}
